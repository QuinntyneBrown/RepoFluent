# Payments guidance

Payment handlers may be analyzed. The `private` directory is excluded from
authoring and shall not support curriculum claims.

```repofluent-policy
{"analysis":"allow","exclude":["private/**"]}
```
