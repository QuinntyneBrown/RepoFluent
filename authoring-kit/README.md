# RepoFluent authoring kit

Released authoring kits live under `releases/<kit-version>`. Each release is a
self-contained, checksummed directory that can validate curriculum packages
without network access after acquisition.

The current prerelease is [`0.1.0`](releases/0.1.0/README.md).
