@startuml
!include <C4/C4_Container>
LAYOUT_WITH_LEGEND()
title Containers — Start governed attempts

Person(actor, "Learner", "Completes formative and summative assessments")
System_Boundary(repofluent, "RepoFluent") {
  Container(entry, "Assessment application", "Angular 21", "Assessment and mastery interface")
  Container(api, "Assessment API", "ASP.NET Core", "Applies application and domain policy")
  ContainerDb(store, "Assessment evidence store", "<TO SUPPLY>", "Stores tenant-scoped state and evidence")
}
System_Ext(dependency, "Time service", "Provides trusted assessment timing")

Rel(actor, entry, "Uses", "HTTPS or local invocation")
Rel(entry, api, "Sends typed requests", "JSON/HTTPS or local contract")
Rel(api, store, "Reads and writes", "Application abstraction")
Rel(api, dependency, "Calls through approved adapter")
@enduml
