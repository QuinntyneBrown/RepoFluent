@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Constrain agent scope

Person(agent, "Curriculum authoring agent", "Produces a package only from an approved source boundary")
System(kit, "Acquired RepoFluent authoring kit", "Applies local instructions, scope preflight, and package validation")
System_Ext(repository, "Approved repository snapshot", "Local code and documents governed by repository instructions")

Rel(agent, kit, "Invokes with a source-scope declaration", "Local command")
Rel(kit, repository, "Reads admitted regular files only", "Local file access")
@enduml
