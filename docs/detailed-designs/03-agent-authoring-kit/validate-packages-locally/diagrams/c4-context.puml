@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Validate packages locally

Person(agent, "Curriculum authoring agent", "Validates generated curriculum before release")
System(kit, "Acquired RepoFluent authoring kit", "Validates one local package with bundled contract artifacts")
System_Ext(workspace, "Approved authoring workspace", "Holds generated package bytes inside the authorized environment")

Rel(agent, kit, "Invokes noninteractive validation")
Rel(kit, workspace, "Reads one declared package from")
Rel(kit, agent, "Returns safe issues and deterministic status", "Standard output")
@enduml
