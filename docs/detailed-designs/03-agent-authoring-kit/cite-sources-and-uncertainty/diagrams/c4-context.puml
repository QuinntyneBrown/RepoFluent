@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Cite sources and uncertainty

Person(agent, "Curriculum authoring agent", "Associates curriculum claims with approved evidence")
System(kit, "Acquired RepoFluent authoring kit", "Validates snapshot citations and structured uncertainty locally")
System_Ext(repository, "Approved repository snapshot", "Provides effective documents and source files")

Rel(agent, kit, "Submits evidence ledger and scope", "Local command")
Rel(kit, repository, "Resolves admitted citations against", "Local file access")
Rel(kit, agent, "Returns safe validation findings", "Standard output")
@enduml
