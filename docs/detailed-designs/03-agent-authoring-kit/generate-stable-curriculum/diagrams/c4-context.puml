@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Generate stable curriculum

Person(agent, "Curriculum authoring agent", "Regenerates semantic entities and completes a package")
System(kit, "Acquired RepoFluent authoring kit", "Derives stable identifiers and finalizes a safe generation manifest")
System_Ext(workspace, "Approved authoring workspace", "Holds identity input, run metadata, and validated package bytes")

Rel(agent, kit, "Invokes local generation commands")
Rel(kit, workspace, "Reads approved local files from")
Rel(kit, agent, "Returns identities, findings, and manifest", "Standard output")
@enduml
