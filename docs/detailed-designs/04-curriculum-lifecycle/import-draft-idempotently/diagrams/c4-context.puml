@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Import a draft idempotently

Person(author, "Curriculum author", "Uploads portable curriculum packages and receives tenant draft results")
System(repofluent, "RepoFluent", "Converges package replays and converts valid content into one complete tenant draft")
System_Ext(source, "Approved package source", "Provides stable package identity, semantic version, and JSON bytes")

Rel(author, source, "Obtains approved package from")
Rel(author, repofluent, "Uploads or replays package")
@enduml
