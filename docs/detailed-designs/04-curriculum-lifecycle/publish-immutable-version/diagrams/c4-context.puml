@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Publish an immutable version

Person(admin, "Curriculum administrator", "Publishes approved unchanged curriculum")
Person(learner, "Learner", "Uses assignments bound to immutable published versions")
System(repofluent, "RepoFluent", "Activates one version, stores publication evidence, and retains historical versions")

Rel(admin, repofluent, "Publishes approved draft", "HTTPS")
Rel(learner, repofluent, "Reads assigned version content", "HTTPS")
@enduml
