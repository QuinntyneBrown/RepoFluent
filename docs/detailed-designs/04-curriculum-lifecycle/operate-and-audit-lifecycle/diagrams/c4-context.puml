@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Operate and audit the lifecycle

Person(operator, "Curriculum operator", "Author or Administrator who monitors and recovers curriculum processing")
Person(auditor, "Auditor", "Inspects tenant-scoped immutable lifecycle evidence")
System(repofluent, "RepoFluent", "Validates, governs, publishes, compares, and retires curriculum packages")

Rel(operator, repofluent, "Monitors state and requests authorized recovery")
Rel(auditor, repofluent, "Retrieves ordered lifecycle evidence")
@enduml
